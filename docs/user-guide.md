# User Guide

1. Prepare a dataset with `jobs.csv` and source images.
2. Configure `project.yaml` or environment variables.
3. Run diagnostics and preflight.
4. Launch `python main.py`.
5. Review outputs under `outputs/`.
