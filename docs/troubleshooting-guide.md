# Troubleshooting Guide

- Run `python diagnose.py` for environment and project checks.
- Ensure `ffmpeg` and `ffprobe` are on PATH.
- For GPU OOM, reduce resolution or use the `low_vram` profile.
- For Drive failures, verify credentials and permissions.
