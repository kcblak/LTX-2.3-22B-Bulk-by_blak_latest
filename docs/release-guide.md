# Release Guide

1. Update `CHANGELOG.md`.
2. Update semantic version in `core/version.py` and `pyproject.toml`.
3. Run diagnostics, tests, and packaging validation.
4. Publish benchmark artifacts for the release candidate.
